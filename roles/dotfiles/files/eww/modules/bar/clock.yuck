; ##########################################
; ### clock
; ##########################################

(defwidget bar_clock []
  (button
    :onclick "eww open calendarmenu"
    :class "bar-button"
    (label
      :text {formattime(EWW_TIME, "%d %B, %H:%M, Week %V" )}
    )
  )
)
