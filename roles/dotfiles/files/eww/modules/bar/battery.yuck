; ##########################################
; ### battery
; ##########################################

(defwidget bar_battery []
  (button
    :class "bar-button"
    (label
      :text "${battery.state == "" ? "" : "${battery.percentage}% "}"
    )
  )
)
